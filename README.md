# The-Local-First-Auth-Engine
Permission-Based System Manager. This isn't just a "To-Do" list; it is a logic engine that determines what a user can see, edit, and save based on their Authentication and Authorization levels, all synced via Context API and Local Storage.

Steps to Run:
->download or clone the repo.
->install all the dependencies from the package.json.
->npm run dev.
